import { getPlatforms, LifeCycleCallback } from "@ionic/react";

export default class Auth {
    static Reg(first_name: string, surname: string, second_name:string, password: string, role: boolean, useIonViewDidEnte: (callback: LifeCycleCallback, deps?: any[]) => void) {
        //Проверяем платформу, на которой запущено приложение
        
    }

    static async Login() {

    }
}